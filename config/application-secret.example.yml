# 将此文件复制为同目录下的 application-secret.yml，用于本地或服务器部署。
# 不要提交 application-secret.yml。
family-food:
  secret:
    key-id: local-v1
    # 32 字节随机值，使用 Base64 编码。生成示例：
#    window系统powershell中执行以下命令
#    $bytes = New-Object byte[] 32
#    $rng = [System.Security.Cryptography.RandomNumberGenerator]::Create()
#    $rng.GetBytes($bytes)
#    $rng.Dispose()
#    $key = [Convert]::ToBase64String($bytes)
#    $key
    master-key-base64: REPLACE_WITH_BASE64_32_BYTE_KEY
